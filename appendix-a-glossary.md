# 附录 A　术语表（中英对照）

> 用法说明：本表汇总全书术语，与正文用词完全一致。正文中每个术语**首次出现**时会给出中英对照与解释；此后出现一律使用本表译名或保留英文。表中"首现章"指该术语第一次出现在正文的章节（可能只是预告式提及，正式讲解章见"出处"列）。Cordis 专有名词（fiber、epoch、isolate、waterfall、serial、bail、effect 等）**不翻译**，保留英文，首次出现附中文解释。

## A.1　框架通用术语

| 英文 | 中文（统一） | 首现章 | 说明 |
|---|---|---|---|
| context | 上下文 | 1 | `Context` 类保留英文；指依赖容器实例，见第 2 章 |
| service | 服务 | 1 | 可被注入的具名能力，`ctx.get` / `inject` 的对象 |
| plugin | 插件 | 1 | 实现 `Plugin` 接口、声明依赖并产生可逆副作用的模块 |
| registry | 注册表 | 1 | 记录服务/插件/监听器及其所有权的容器 |
| event | 事件 | 1 | 通过 `ctx.on`/`ctx.emit` 分发的消息，见第 4 章 |
| adapter | 适配器 | 1 | 把统一接口翻译到具体实现的插件（如 LLM 适配器、驱动） |
| provider | 提供方 | 3 | 实现某个抽象能力（接口）的插件；与消费方（consumer）相对 |
| consumer | 消费方 | 2 | 声明依赖某能力的插件，见 2.4 节 |
| scope | 作用域 | 2 | 隔离/归属上下文，见 11.x 与 12.x 的作用域讨论 |
| registry（扩展） | 注册表 | 1 | 亦指主机注册表（AgentRegistry、ProviderRegistry） |

## A.2　Cordis 专有名词（保留英文）

| 英文 | 中文（唯一编译） | 首现章 | 说明 |
|---|---|---|---|
| fiber | 纤维（生命周期单元） | 1 | 一个条目（entry）在容器中的生命载体，见第 3 章 |
| epoch | 纪元 | 1 | 负载/依赖的代际编号，用于检测"已过期"的加载 |
| effect | 效应（副作用登记） | 1 | 副作用的可撤销登记项，`ctx.effect` 注册 |
| isolate | 隔离（保持英文） | 1 | 依赖隔离边界，见 5.x |
| intercept | 拦截（保持英文） | 1 | 事件分发的环绕语义，见第 4 章 |
| extend | 扩展（保持英文） | 1 | 插件对服务/事件的扩展点 |
| waterfall | 瀑布（保持英文） | 1 | 环绕式分发：监听器可改写载荷，见 4.4 节 |
| serial | 串行（保持英文） | 1 | 串行分发，见 4.3 节 |
| parallel | 并行（保持英文） | 1 | 并行分发，见 4.2 节 |
| bail | 短路（保持英文） | 1 | 分发提前终止的返回值，见 4.1 节 |

## A.3　会话与 Agent 领域术语

| 英文 | 中文（统一） | 首现章 | 说明 |
|---|---|---|---|
| session | 会话 | 1 | 一次持久化的对话历史 + 生命周期，见第 8 章 |
| turn | 轮次 | 1 | 一次模型请求-响应周期，见第 9 章 |
| step | 步骤 | 1 | 轮次内的一次工具调用-结果循环，见 9.x |
| message | 消息 | 1 | 模型对话中的一条内容（role + content） |
| stream / streaming | 流式 | 1 | 增量输出/增量事件 |
| phase | 相位 | 9 | 轮次状态机（如 executing/responding），见 9.2 |
| inbox | 收件箱 | 8 | 会话的入站消息投影，见 9.x |
| wake | 唤醒 | 9 | "入站有新消息，请再看一眼"的通知信号 |
| latch | 锁存 | 9 | 一次性置位、持续到被消费的状态位（收敛锁存） |
| barrier | 屏障 | 9 | 并发工具执行的分批边界 |
| 滚动池（rolling pool） | 滚动池 | 9 | 并行工具调度的限额窗口 |
| initiator | 发起方 | 9 | 事件因果链的源头（agent 或用户） |
| projection | 投影 | 7 | 从事件流派生的只读视图（历史/表面/收件箱），见 8.6 |
| snapshot | 快照 | 1 | 冻结的浅拷贝/深拷贝对象，用于事件载荷 |
| surface | 表面 | 5 | 会话对外暴露的可验证视图，见 8.5 |
| compaction | 压缩 | 6 | 长会话的旧事件折叠/摘要，见第 14 章 |
| checkpoint | 检查点 | 3 | 可恢复状态的一致性标注点，见 14.x 与 15.x |
| seed | 种子 | 8 | 会话恢复时注入的"前史"事件序列，见 8.7 |
| restore | 恢复 | 8 | 从持久化后端重建会话 |
| fork | 分支 | 8 | 以源会话前缀为种子创建子会话 |
| replay | 回放 | 8 | 以纯函数对事件流重演（折叠/派生） |
| guard | 守护 | 14 | 超时/重复提醒等护栏插件，见第 14 章 |

## A.4　配置与组装术语

| 英文 | 中文（统一） | 首现章 | 说明 |
|---|---|---|---|
| profile | 配置档案 | 1 | 一组配置文件（bundles + 补丁 + 插件行），见第 6 章；能力语境下另有"能力剖面"义（如 ACP narrow profile，见 16.8） |
| patch | 补丁 | 1 | 对目标行的整行替换配置，见 5.4 |
| bundle | 组合包 | 1 | 预组装的可复用插件集合（如 dsh-base），见第 7 章 |
| preset | 预设 | 1 | 会话级配置模板（如 agent 预设），见 7.x 与 13.x |
| overlay | 叠层 | 1 | 运行期叠加的补丁来源（`--patch`、home 层） |
| entry | 条目 | 1 | 配置文件中的一行插件描述 |
| driver | 驱动器 | 7 | 提供方/执行器的具体实现（如 in-process-driver） |
| seam | 接缝 | 7 | 能力抽象层（服务声明包/实现包/消费包），见第 12 章 |
| roster | 花名册 | 7 | preset 的注册清单 |
| pty | 伪终端 | 6 | 终端仿真层（acp-agent 的 pty.cordis.yml） |
| HMR | 热替换 | 3 | 热模块替换：变更即重新装配 |
| trust | 信任级别 | 5 | preset 目录的可信来源分级 |

## A.5　安全与可靠性术语

| 英文 | 中文（统一） | 首现章 | 说明 |
|---|---|---|---|
| sandbox | 沙箱 | 1 | 受限执行环境（文件/进程/网络） |
| approval | 审批 | 7 | 用户对危险操作的许可流程，见 12.7 |
| credential | 凭据 | 1 | API key 等敏感配置，见第 15 章 |
| 权限预设 | 权限预设 | 7 | read-only / workspace-write / danger-full-access 三档 |
| veto | 否决 | 8 | 监听器同步抛错击退挂载的机制 |
| rollback | 回滚 | 3 | 卸载时逆序撤销副作用的机制 |

## A.6　多智能体、产品与协议术语

| 英文 | 中文（统一） | 首现章 | 说明 |
|---|---|---|---|
| subagent | 子代理 | 7 | 由代理发起的次级代理，见第 13 章 |
| skill | 技能 | 7 | 前置知识/指令包，见第 13 章 |
| job | 任务 | 7 | 可排队执行的后台任务，见第 13 章 |
| workflow | 工作流 | 7 | 多步骤编排，见第 13 章 |
| SDK | SDK（软件开发包） | 16 | 宿主/客户端协议封装，见第 16 章 |
| ACP | ACP（代理协议） | 16 | 与外部代理互操作的标准协议，见 16.8 |
| typert | typert（保持英文） | 16 | 网关类型生成器（@Remote），见 16.6 |
| webserver / WS | Web 服务器 / 双向流 | 16 | 浏览器宿主传输，见第 16 章 |
| api gateway | API 网关 | 16 | @Remote 暴露面生成物 |
